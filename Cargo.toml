[workspace]
members = ["crates/rrc-fetch", "crates/rrc-manifest", "crates/rrc-run"]
resolver = "3"

[workspace.package]
version = "0.0.2"
edition = "2024"
rust-version = "1.98"
license = "Apache-2.0"
repository = "https://github.com/tamnd/rucc-real-corpus"

[workspace.dependencies]
rrc-fetch = { path = "crates/rrc-fetch", version = "0.0.2" }
rrc-manifest = { path = "crates/rrc-manifest", version = "0.0.2" }
rrc-run = { path = "crates/rrc-run", version = "0.0.2" }
regex = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.9"
serde_json = "1"
sha2 = "0.10"

[workspace.lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"

[profile.release]
lto = "thin"
strip = true
